{
  "schema_version": 1,
  "equivalent_mutants": [
    {
      "module": "src/host/work-type-gates.mjs",
      "line": 136,
      "guard": "< -> <=",
      "reason": "Unreachable with the value that would distinguish it. `deltaRatio < 0 ? 'improved' : 'regressed'` is only evaluated when `Math.abs(deltaRatio) >= noiseThreshold`, and the threshold is demanded to be a positive number, so a delta of exactly zero always returns 'inconclusive' on the line above. `<=` and `<` therefore agree on every input that reaches this line."
    },
    {
      "module": "src/host/tickets-manifest.mjs",
      "line": 213,
      "guard": "< -> <=",
      "reason": "The outer loop body is nothing but the inner loop, whose bound is `j = i + 1; j < tickets.length`. With `i === tickets.length` that condition is false at once, so `tickets[i]` is never dereferenced and the extra iteration does nothing. The mutant and the original visit the same pairs."
    },
    {
      "module": "src/host/tickets-manifest.mjs",
      "line": 239,
      "guard": "< -> <=",
      "reason": "The comparator's two branches differ only when two ticket ids are equal, and `tickets_duplicate_id` refuses a manifest with a repeated id before any digest is taken. The property the sort exists for \u2014 the same set in another order gives the same digest \u2014 is tested directly."
    },
    {
      "module": "src/host/tickets-manifest.mjs",
      "line": 239,
      "guard": "> -> >=",
      "reason": "The other half of the same comparator, unreachable for the same reason: equal ids cannot occur in a manifest that reaches the projection."
    },
    {
      "module": "src/host/planning-publication.mjs",
      "line": 54,
      "guard": "< -> <=",
      "reason": "The comparator's branches differ only for two equal trailer names, and the list is proven duplicate-free by test: `TRAILERS` and the kind-specific set are disjoint, so no tie is reachable. The property the sort exists for \u2014 one ordering whatever order a host listed them in \u2014 is tested directly."
    },
    {
      "module": "src/host/planning-publication.mjs",
      "line": 54,
      "guard": "> -> >=",
      "reason": "The comparator's branches differ only for two equal trailer names, and the list is proven duplicate-free by test: `TRAILERS` and the kind-specific set are disjoint, so no tie is reachable. The property the sort exists for \u2014 one ordering whatever order a host listed them in \u2014 is tested directly."
    },
    {
      "module": "src/host/governance-bundle.mjs",
      "line": 76,
      "guard": "< -> <=",
      "reason": "The comparator's branches differ only for two members with the same path, and `inventoryErrors` refuses a bundle whose manifest repeats one. The property the sort exists for \u2014 one digest whatever order the members arrive in \u2014 is tested directly."
    },
    {
      "module": "src/host/governance-bundle.mjs",
      "line": 76,
      "guard": "> -> >=",
      "reason": "The comparator's branches differ only for two members with the same path, and `inventoryErrors` refuses a bundle whose manifest repeats one. The property the sort exists for \u2014 one digest whatever order the members arrive in \u2014 is tested directly."
    },
    {
      "module": "src/host/project-fs.mjs",
      "line": 32,
      "guard": "<= -> <",
      "reason": "The two differ only when the separator is at position zero \u2014 a target directly under the filesystem root \u2014 where `'/'` and `''` both produce a resolved path outside any project root. The one root that would tell them apart is `/` itself, and the adapter now refuses that at construction (`fs_outside_project`), with a test. So no admitted project can reach an input where the branches disagree."
    },
    {
      "module": "src/host/delta-driver.mjs",
      "line": 88,
      "guard": "<= -> <",
      "reason": "The same one-line helper, used only on a temporary index path built by this module under a directory it created. That path always has a separator past position zero, so the zero case \u2014 the only place the branches differ \u2014 is unreachable from any call site."
    },
    {
      "module": "src/host/delta-driver.mjs",
      "line": 152,
      "guard": "< -> <=",
      "reason": "Paths in a tree listing are unique by construction \u2014 a tree cannot hold one path twice \u2014 and the declared-entry loop de-duplicates before this sort runs. The comparator's tie branch is therefore unreachable, and the ordering property is tested directly."
    },
    {
      "module": "src/host/delta-driver.mjs",
      "line": 152,
      "guard": "> -> >=",
      "reason": "Paths in a tree listing are unique by construction \u2014 a tree cannot hold one path twice \u2014 and the declared-entry loop de-duplicates before this sort runs. The comparator's tie branch is therefore unreachable, and the ordering property is tested directly."
    },
    {
      "module": "src/host/approved-delta.mjs",
      "line": 81,
      "guard": "< -> <=",
      "reason": "The same comparator over a path list that `containment_mismatch` refuses when a path appears twice, so equal paths cannot reach it. The property \u2014 one canonical order whatever order the entries arrived in \u2014 is tested."
    },
    {
      "module": "src/host/approved-delta.mjs",
      "line": 81,
      "guard": "> -> >=",
      "reason": "The same comparator over a path list that `containment_mismatch` refuses when a path appears twice, so equal paths cannot reach it. The property \u2014 one canonical order whatever order the entries arrived in \u2014 is tested."
    },
    {
      "module": "src/host/finding-registry.mjs",
      "line": 79,
      "guard": "< -> <=",
      "reason": "A comparator over registry keys, which are unique by construction: the entries come from a Map. Equal keys cannot occur, so the tie branch is unreachable."
    },
    {
      "module": "src/host/finding-registry.mjs",
      "line": 79,
      "guard": "> -> >=",
      "reason": "A comparator over registry keys, which are unique by construction: the entries come from a Map. Equal keys cannot occur, so the tie branch is unreachable."
    },
    {
      "module": "src/host/verify-doc-runner.mjs",
      "line": 116,
      "guard": "=== -> !==",
      "reason": "`reported_as` is read by `classifyRun` only when `doctor_outcome === 'failed'`, where the single guard refuses the value `product_failure`. Both the original and the mutant supply something else there \u2014 `infrastructure_failure` or `undefined` \u2014 so the classification and the returned outcome are identical for every input."
    },
    {
      "module": "src/host/artifact-classifier.mjs",
      "line": 95,
      "guard": "< -> <=",
      "reason": "The comparator's second key breaks ties in specificity by class name, and two entries with the same specificity *and* the same class name would be one class registered twice \u2014 which `ambiguous_class` refuses before any path is classified. The tie branch is therefore unreachable, and the selection property is tested directly."
    },
    {
      "module": "src/host/workflow-graph-canonical.mjs",
      "line": 89,
      "guard": "> -> >=",
      "reason": "Unreachable difference under a stable sort. The comparator is `left < right ? -1 : left > right ? 1 : 0`, so its two remaining branches can only disagree on equal keys, where the original answers 0 and the mutant answers 1 \u2014 both meaning \"do not move left before right\". A stable sort never moves an element past one it does not compare strictly-before, so the permutation is identical: measured on runs of equal keys of length 2, 3, 4, 5, 10 and 15, and on mixed keys containing a duplicate, the original and the mutant agree on every one. The sibling mutant on this line, `< -> <=`, answers -1 on equal keys and does reorder them; it is killed by \"two members a document should not have keep the order it wrote them in\", so the line is covered and this is the single difference no input can show."
    }
  ],
  "non_terminating_mutants": [
    {
      "module": "src/host/artifact-classifier.mjs",
      "line": 118,
      "guard": "> -> >=",
      "reason": "`while (queue.length > 0)` is the only exit of `impactClosure`. `> -> >=` makes an empty queue (`length === 0`) still enter the body. `queue.shift()` then yields `undefined`; `byName.get(undefined)?.impacts ?? []` is empty, so nothing is enqueued and the condition stays true. `seen` only suppresses re-enqueue of a named class \u2014 it is not the loop condition \u2014 so even a cycle-free registry hangs once the work list drains. The mutant never returns an impact set."
    },
    {
      "module": "src/host/tickets-manifest.mjs",
      "line": 131,
      "guard": "> -> >=",
      "reason": "`while (ready.length > 0)` is Kahn termination in `kahnOrder`: empty `ready` means the remaining nodes are a cycle (or the graph is done). `> -> >=` keeps that empty list in the body. `ready.shift()` yields `undefined`, which is pushed onto `order` every iteration; `children.get(undefined) ?? []` adds nothing back. The mutant never returns, so `graphErrors` cannot observe `order.length !== tickets.length` and the cycle refusal is unreachable."
    },
    {
      "module": "src/host/tickets-manifest.mjs",
      "line": 190,
      "guard": "> -> >=",
      "reason": "`while (stack.length > 0)` is the only way `reaches` inside `comparable` returns false. `> -> >=` keeps an empty stack in the body. `stack.pop()` yields `undefined`; `byId.get(undefined)?.depends_on ?? []` is empty, so the `parent === to` hit and the `stack.push` never run, and `return false` after the loop is unreachable. `seen` only skips parents already walked \u2014 it does not stop the loop \u2014 so a fully explored (even acyclic) walk hangs instead of answering."
    },
    {
      "module": "src/host/staging-lineage.mjs",
      "line": 34,
      "guard": "demand(",
      "reason": "`demand(!seen.has(receipt.staging_commit_oid), 'containment_mismatch', \u2026)` is the only break of a receipt cycle in `lineageFor`. Neutralising it leaves `while (cursor !== head)` walking `cursor = receipt.staging_commit_oid`. On a loop (a\u2192b, b\u2192a) every cursor is a key in `byBase`, so `if (!receipt) break` never fires, and `head` off the cycle (the existing test uses `z`) is never reached. `seen.add` of a repeat is a no-op and is not a loop condition. The test that feeds that loop is correct \u2014 it asserts `containment_mismatch` \u2014 but without the demand the walk does not return a value to assert."
    }
  ]
}
